# 崇明青少年户外探索平台项目要求

## 1. 项目定位（已明确）

本项目拟建设一个以用户生成内容（UGC）为主的崇明户外探索平台，帮助用户发现具有直观吸引力的具体打卡点，将多个打卡点组织为游览路线，并围绕出行计划寻找同行者，促进用户从产生兴趣走向实际出行。

平台以“打卡点”为核心内容单位。用户可进一步选择多个打卡点组成路线。

## 2. 目标用户（已明确）

平台面向对崇明户外游览感兴趣的个人用户，重点关注青少年及其家庭，同时允许其他具有真实体验的用户参与内容贡献。

## 3. 核心价值（已明确）

平台的核心价值是帮助用户快速发现感兴趣的打卡点，便捷地找到具有共同出行意向的同行者，并以较低的组织成本形成轻量而完整的短途出行计划，促进计划实际成行。

相较于以一般图文分享或大型景点介绍为主的产品，本项目在设计上具有以下差异：

- 更具体的内容组织粒度：
  UGC 围绕具有明确位置和直观吸引力的打卡点组织，并允许用户将打卡点进一步组织为路线。
- 从内容发现到出行安排的连续流程：
  打卡点可直接作为路线节点及行程对象，继续用于路线组织、时间安排和同行邀约，使用户无需在浏览内容后重新整理出行信息。

## 4. 核心使用场景（已明确）

### 4.1 出行规划

用户产生短途出行意愿或正在准备一次出行时进入平台，发现和筛选感兴趣的打卡点，将其组织为路线，确定时间安排，并视需要邀请同行者。

### 4.2 碎片时间浏览

用户在通勤、候车或其他空闲时间进入平台，以低负担方式浏览打卡点并获取出行灵感。用户在有备选打卡点列表时，也可以进一步形成路线、安排时间或寻找同行者。

两类场景分别对应无明确目的时的兴趣发现和明确目的下的主动规划，并最终汇入从打卡点发现到出行安排的核心流程。

## 5. 内容与信息结构（已明确）

平台现阶段包含人、安排、打卡点和路线四类主要数据对象。
同时存在用户关系、邀请等辅助数据对象，但不将其列为主要数据对象。

### 5.1 人

人表示平台中的个人用户。公开资料、实名资料和认证结果应相互区分；安排、聊天和用户关系不完整嵌入用户数据，而是通过用户 ID 与其关联。

```text
User #用户
- id 
- Profile #个人信息档案
- Privacy #个人信息，出行计划等信息的默认公开性配置。分别设置private/public/restricted等
- identityInformation #实名身份信息#隐私
- verificationInformation #家长/学生等身份
- accountStatus #账号状态
- createdAt #记录创建时间
```

其中，`Profile` 用于保存昵称、头像和简介等资料，公开性可选；`identityInformation` 与 `verificationInformation` 分别表示身份信息和认证结果。

### 5.2 安排

安排表示某位用户在特定时间对一个打卡点或一条路线形成的个人计划。安排独立存储，用户日历根据所属用户和日期查询并显示安排。

```text
Schedule #安排
- visibility: private | public | restricted
- id
- ownerUserId #所属
- startTime #计划开始时间
- endTime #计划结束时间
- target #schedule的对象，可能包含单独打卡点或导入的路线等，schedule中保存快照。
  - type: checkpoint | route
  - version #来源对象版本
  - id #来源对象 ID
  - snapshot #当前对象的快照
- planStatus #已确认/已完成/已取消等状态字段
- recruitmentStatus #是否接受新成员，对新成员要求等状态字段
- sharedNote #集合点、共同说明等，接受邀请时随安排复制
- privateNote #个人备注，仅由所属用户维护，不随邀请复制
- sourceInvitationId（可选） #如来自其他人邀请，可保留复制源信息
- createdAt #创建时间
```

打卡点快照至少保留打卡点 ID、标题和定位；路线快照至少保留路线标题、说明及有序节点，每个节点保留打卡点 ID、标题、定位和分段交通方式。快照用于保证已确认的安排不因来源对象后续修改而变化。

结构化邀请应包含安排中的共同信息；对方接受邀请后，系统复制目标快照、时间、状态及 `sharedNote` 等共同内容并生成新的个人安排，`privateNote` 不参与复制。

### 5.3 打卡点

打卡点是一张适合碎片时间浏览的即时发现卡片，以少量信息表达一个具体位置的吸引力。

```text
Checkpoint #打卡点
- id
- version
- authorUserId #打卡点创建者id
- location #定位信息
  - latitude
  - longitude
  - locationName
  - coordinateSystem
- title #标题
- shortText #短文本
- imageId #图片源
- tagIds[] #标签
- publishStatus #审核状态
- createdAt #记录创建时间
```

面向用户的核心内容为一张图片、标题、一句话正文、标签和定位。

### 5.4 路线

路线是由用户创建的、按顺序连接多个打卡点的轻量内容对象。平台保存打卡点链及其顺序，不将导航服务生成的具体道路路线作为核心数据。

```text
Route #路线
- id
- version
- authorUserId #路线创建者id
- stops[] #构成路线的打卡点信息
  - checkpointId #什么打卡点？
  - travelModeFromPrevious #前往当前点出行方式信息（第一站应当留空）
- title #路线标题
- text #说明等交代
- tagIds[] #标签
- publishStatus #审核状态
- createdAt #创建时间
```

`stops` 必须保存为有序节点数组。节点保留为小型结构，以便后续按需增加预计停留时间、分段备注等信息。

未来可考虑将有序打卡点链提供给导航 App，用于便捷路径规划；该功能优先级较低，不属于当前原型必须完成的内容。

### 5.5 对象关系

```text
User
├── 创建 Checkpoint
├── 创建 Route
└── 拥有 Schedule

Route
└── 有序引用多个 Checkpoint

Schedule
└── 引用一个 Checkpoint 或 Route
```

对象之间通过稳定 ID 建立关联，逻辑关系可以双向查询。

## 6. 功能范围与优先级（已明确）

### 6.1 原型预期功能

#### 发布打卡点

用户可以填写定位、标题、一句话正文、标签和一张图片，提交并生成一张打卡点卡片。

#### 组织路线

用户可以选择多个打卡点、调整游览顺序并组成路线。平台保存有序打卡点链信息；具体道路路径规划属于未来的低优先级扩展，不属于当前原型必须完成的内容。

#### 创建安排

用户可以围绕一个打卡点或一条路线选择日期及时间、设置计划状态和招募状态，并在个人日历中形成安排。

#### 发现同行者并确认安排

用户可以在打卡点页面查看具有公开共同出行意向的用户，通过消息沟通，并以结构化邀请确认一次安排。接受邀请后，邀请中的安排被复制到接收者的个人日历，双方可以分别维护自己的安排和个人备注。

同行候选的暂定范围包括：直接为该打卡点创建公开招募安排的用户；为包含该打卡点的路线创建公开招募安排的用户。后一种情况应标注为“计划路线包含此打卡点”，以区别于仅围绕单个打卡点创建的安排。候选用户的具体排序、筛选和推荐规则留待后续细化。

## 6.2 核心功能链路

```text
发布并发现打卡点
→ 将多个打卡点组织为路线
→ 围绕打卡点或路线创建安排
→ 在打卡点页面发现具有共同意向的用户
→ 通过消息沟通并发送结构化邀请
→ 对方确认后复制进个人日历安排
```

聊天消息用于沟通，结构化邀请用于承载可确认的具体安排；只有明确接受邀请后，系统才生成接收者的日历安排。

### 6.3 后续细化内容

以下内容不影响当前核心流程，需要更多时间完善，部分需要商讨后决定：

- 隐私状态的定义与范围(需商讨)
- 打卡点与路线修改之后的更新与版本管理系统；
- 时间冲突的具体判断与自动过滤规则；
- 同行候选的排序、筛选及推荐方式；（需商讨）
- 招募人数、剩余名额和可见范围；（需商讨）
- 消息与邀请的具体交互；
- 安排变更、退出、取消及重新确认；
- 身份认证或家庭关系对请求的限制；（需商讨）
- 完整的地图路线生成与异常处理。（需商讨）

## 7. 内容治理与用户安全（需商讨）

- 内容审核范围和基本原则：
- 身份认证及关系绑定的使用范围：
- 邀请、加入和确认请求的基本规则：
- 位置、时间及个人信息的展示原则：
- 举报、纠错及异常内容处理方式：

##### 该部分如需要真正上线，是否可以完全托管？我们计划提供平台其他的骨架，审查规则，身份验证等部分，保留接口是否可行？

## 8. 页面与交互结构

- 主要页面：
- 页面之间的核心流程：
- 打卡点等碎片化信息的组织方式：
- 路线编辑与搭伙入口：
- 当前优先完成 Android 移动端演示原型；桌面端暂不纳入本阶段范围。

我们之后确认完之后，直接从这里入手解决，自然完成这一部分。

## 9. 内容准备

- 示例打卡点；
- 示例路线；

## 10. 技术与交付要求

- 最终交付形式：可交互 Android 演示原型。原型采用本地持久化数据和模拟用户数据，真实实现打卡点发布、路线组织、安排创建及邀请确认等核心业务状态变化；真实账户体系、服务端、多用户实时通信和自动审核等功能暂不实现。
- 技术栈或平台限制：优先完成 Android 移动端演示原型
- 是否接入真实地图、账户及后端服务：否
- 视觉风格要求：如demo展示，可调整
- 验收标准：待商议

## 11. 当前工作

当前工作以完成正式需求、完整内容骨架和少量参考案例为主。
